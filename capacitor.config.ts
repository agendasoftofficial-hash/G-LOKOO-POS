import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {appId:'com.agendasoft.glokoo.pos',appName:'G-LOKOO POS',webDir:'dist',server:{androidScheme:'https'}};
export default config;
